<!-- Update from the repository root: UPDATE_WORKBENCH_SNAPSHOTS=1 go -C tools/workbench-go test ./acceptance -run '^TestWorkbenchFreshSetupNarrative$' -count=1 -->
# Workbench setup narrative

## Fresh setup

A fresh corrected PackageScope Subject assembles its two independently governed repositories into a Workbench.

```console
$ workbench setup
Workbench reconciled 2 repositories; 10 generated paths changed.
```
