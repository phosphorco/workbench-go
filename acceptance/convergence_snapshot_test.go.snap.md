<!-- Update from the repository root: UPDATE_WORKBENCH_SNAPSHOTS=1 go -C tools/workbench-go test ./acceptance -run '^TestWorkbenchConvergenceNarrative$' -count=1 -->
# Workbench setup narrative

## Identical second setup

The identical request converges without changing an owned projection.

```console
$ workbench setup
Workbench reconciled 2 repositories; 0 generated paths changed.
```
